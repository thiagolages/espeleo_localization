/*
 * Copyright 2015-2018 The MathWorks, Inc.
 */

try {
    project.catkin.tree.generateMessageArtifact(project, 'ar_track_alvar_msgs')
} catch (NullPointerException e) {
    println("Couldn't find ar_track_alvar_msgs on the ROS_PACKAGE_PATH")
}

task bugfixtask << {
    mkdir sourceSets.main.output.classesDir
}
jar.dependsOn(bugfixtask)
